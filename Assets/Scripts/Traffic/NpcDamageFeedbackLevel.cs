/// <summary>Visual/audio feedback tier derived from remaining health fraction. Purely a display concern — never affects damage, death, or gameplay math.</summary>
public enum NpcDamageFeedbackLevel {
    None,
    Smoking,
    Critical
}
